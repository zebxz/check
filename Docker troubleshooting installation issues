l know that today github reported that they had an issue


INSTALLING DOCKER FOR DESTOP ON MY WINDOWS

issues:
Virtualization support not detected

1) Resolution for Virtualization issue
Go to:
windows logo
Settings & security
Recovery
Advanced start-up: Restart now 
f10
advanced
Systems options
then i tick; virtualization (VTx)
I save & exit
Then go to Task manager; select perfomance 
and i see virtualization enabled


issue:
DOCKER ENGINE STUCKED AT STARTING

2) installing Wsl, windows subsystem for linux
chatgpt said in the windows terminal run: wsl -l -v
i ran it but it said : 
windows subsystem for Linux has no installed distribution 
you can resolve this by installing a distribution with the instructions below:
use 'wsl.exe --list -online' to list available distributions
and 'wsl.exe --install <Distro>

so i ran: wsl.exe --install
Provisioning the new WSL instance ubuntu
Create a default Unix user account : ekehc
Password: Meekyways
distribution successfully installed. It can be launched via 'wsl.exe -d Ubuntu'

***STill stucked ...
shut down Docker
B)   Disable control flow guard
Go to setings
systems & security; windows security
App & browser control 
scroll to exploit protection; program settings
look for:   C:\WINDOWS\System32\vmcompute.exe , if not there, 
look up and click add programme to customize; choose exact file path & add C:\WINDOWS\System32\vmcompute.exe 
then scrool down it should hv been added, then right click on it and click on edit
then disable clontrol flow guard
then click apply & close

then in powershell 
run as administrator 
then run ' net start vmcompute
next set the wsl version
wsl --set-default-version 2
then close 

restart computer & docker

**** its stil stucked

issue:
3) Clear corrupted AppData
Docker stores corrupted files in your user profile that arent always removed during uninstallation

A) close docker destop completely (check the task manger to ensure all "Docker" processes are gone 

B) Press Win + R, type %AppData%, and press enter. Delete the docker folder

C) press Win + R again, type %LocalAppData%, and press enter. Delete the Docker folder here as well 

D) Restart the Application.


      ***** RESOLVED***** 



