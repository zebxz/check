
first resolution   ... not successful
  in gitbash, Engr Jude deleted the .gitignore file then pushed to github but in github, the folder/code could not open
For gitbash he deleted it manually but when he was trying to push it he was having authentication issue bc he has a previous gitbash repo & gitbash cant know the diff frm the current gitbash repo 
so he finally deleted the first repo & was able to authenticate into the new repo & pushed it to github


  Second resolution
In github ie the GUI, he deleted the .gitignore file but we can see the commit notification: akpos-tech Delete .gitignore





  proposed resolution
  in gitbash/mobaxterm, create a new .gitignore, copy the content inside the cloned .gitignore, paste it inside the new .gitignore and delete the cloned .gitignore, then push code to github
  
