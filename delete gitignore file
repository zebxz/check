
JLY 24th
first resolution   ... not successful
  in gitbash, Engr Jude deleted the .gitignore file then pushed to github but in github, the folder/code could not open
For gitbash he deleted it manually but when he was trying to push it he was having authentication issue bc he has a previous gitbash repo & gitbash cant know the diff frm the current gitbash repo 
so he finally deleted the first repo & was able to authenticate into the new repo & pushed it to github


  Second resolution
In github ie the GUI, he deleted the .gitignore file but we can see the commit notification: akpos-tech Delete .gitignore



  proposed resolution for next time   ... but we didnt do this 
  in gitbash/mobaxterm, create a new .gitignore, copy the content inside the cloned .gitignore, paste it inside the new .gitignore and delete the cloned .gitignore, then push code to github



    Jly 28th
using gitbash
create dir/folder
cd into the folder
then git clone the code 
on your computer/desktop dir create new folder and give it a name
then manualy transfer thr files into it except the .gitignore folder
then in gitbash cd out
then cd into the new folder
alreday created new repo to push to in github
then git initialize 
then git add
you cud  git add.
then git commit -M 
SHUD BE ON MASTER BRANCH 
GIT REMOTE -V   TO CHECH REPO ATTACHED in gitbash
if non .... tthen git remote add alias
gitbash shud already be authenicated with github
then git push -u   ... for the first push
subsequently pushes you dnt need -u
if u hv nt authenticated it will then ask you for your 
secret token no longer 
authentication token from github













  
