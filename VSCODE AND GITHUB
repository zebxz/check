VSCODE
****do not delete any file  directly under explorer, he said; it will delete all the files..
cd ..    to go back
Esc  & :wq    … to save 
Type clear to clear terminal




1)
To open terminal click on the  3dots after Run

2)
Once the terminal opens click on the arrow pointing downwards & select bash shell 
Git bash has to be installed in my computer then I can see it in my vscode

3)
To install  as admin  ….  eg gitbash
Click the windows logo & the key X
Then select admin
 ASK HIM HOW HE GOT THE LINK & WHEN TO KNOW IF THE LINK CAN BE USED IN THE COMMAND LINE
Also how did you locate the credentials manager you deleted
We were nt able to cd into READ.md because it is a file
We cd into  a dir (colour blue)   but ls into a file the vi into it   
Get a better name for my repo 
 
4) folder is better if created on github for easy copying frm one file to the other
   **I saw this when 



GITHUB
To delete repo 
Click on the 3lines above “code’”, scroll down and select the repo to open it
Next 
Select “Settings”  after insight


To delete   .gitignore
You can use gitbash or in vscode select gitbash in terminal

STEPS:
1)	In github create a repo
Next
2)	On your computer, inside file directory, create a “folder A” in desktop 
Next:
3)	In vscode
Click Open folder & create a “folder B” in desktop
Select the folder & in Vscode make sure you can see the folder (that’s where the code is cloned into)
Open terminal & clone the repo 
.. Next:
4)	On windows desktop;
Copy all the files in “folder B” into “folder A”
Next
5)	In Vscode

Select open folder & select “folder A”  AND run:
 
1)git init
 2)  git add .
  3)  git commit -m "first commit"
  4)  git status
   5)  git remote -v
   7)  git remote add origin https://github.com/Ceceek/MY-project.git
   8)  git remote -v
   9)  git push -u origin master
   10)  clear


Issue resolved
In github, I deleted the”” Landmark logo”” in the READmd  file & committed the flle
Then In vscode, I added smt in the READmd file 

I ran; git add . & git commit -M & git push origin master 
But it said thers a conflict

It suggested I run; git pull
Then I saw the error
I ran; git diff & git merge it didn’t work

It then suggested I; fix the conflict in my working tree the run git add
So I vi into the error message it pulled

And deleted the conflict (indicated as “”<<<<<head “”
Saved & exited

Then 
I ran; git add . & git commit -M & git push origin master 
  **issue resolved…



BETTER RESOLUTION IDEA
To delete the “”Landmark Logo””
 
 Refer to Step 5) above 
In Vscode, once I open the “”folder B”” where I have copied the files into 
After git init
Vi into the “”READmd”” & delete the logo 
Then 
Git add . , git commit -m & git push 



