

What is Cloud Computing ?
Cloud computing is the delivery of computing services—including  

 aws cloud = AWS SERVICES
  - servers,     = ec2 instances  
  - severless,   = Lambda functions,
  - storage,     = ebs, efs, s3, 
  - databases,   = rds [ mySQL, mariaBD, posgressSQL, arura,  ], nonRDS[ dynamoDB,  ]
  - networking,  = vpc, 
  - software,    = ami, SDLC, 
  - analytics,   = CloudWatch, 
  - and more
  - security     = IAM
ELB, vpc, vpc peering, route table, routing traffic, B , ASG/auto scaling, launch template

  Netflix =   S3  bucket


               HOW TO MIGRATE FROM AN ONPREM INFASTRUCTURE TO AWS
There are diff aws services that is used for storing of data
And migrating will neccesitate data been distributed such that data can be accessed with relative used
1) one of the aws services we have used to migrate data is s3 bucket with this data can be moved/migrated bc we can just download all the files in the S3 bucket
eg
using the command AWs s3 cp , i can copy a file from my local server to an s3 remote bucket in Aws once the file is there, it becomes very easy for the file to be accessed
there are many tools that we can use but have talked abt the most basic option to migrate data & that migration will take place using s3 BUCKET

2)Also we cud use an ebs vol, we can create snapshot from the Ebs vol & use it to create new servers with such info which is very important bc if i have data already in my 
database server & extract, i create a snapshot from that particular piece of vol, we can use that snapshot & copy whatever files into a new system completely, 
that is data migration

        MIGRATING DATA FROM AN ONPREM DATABASE TO A CLOUD DATABASE
 first we have to identify how much data has to be migrated, ie frm the on prem to the aws cloud 
so the data shud be hosted by aws, if the data is running on my normal window server all i can do is i can create a new server in Aws, log in & copy the file from server1 to
server2 and how can copy files from one serve to another 
we can use the scp command files & 40.03, directors 

there are other options that can be used that is more complexed like dedicated software for just data migrated 





DOCKER ISSUE:
Deploy app with the wrong image 

DOCKER FILE;
A simple plain text containing instructions to build a docker image

We can scan the docker image using Docker scout, or trivy, integrated into the docker desktop OR Docker CLI

DOCKER IMAGE: 
is a package that contains everything an application needs to run including the application code, dependencies, lib, software, instruction etc.

DOCKER CONTAINER:
Its the instance runtime of a docker image

DOCKER BEST PRACTICES:
1) Using the official docker image as our base image in our docker file
2) Reduce the number of run commands so as to reduce the number of layers in the image inorder to keep the image light weight
3)Scan the image before using
4)Use executable over shell form for commands so that our applictaion will run as a parent process & wont be killed unnoticed.




IAAC:
It is not Prune to errors
saves time because its reuseable /Visibility is very easly  
Great automation - 99%  
It can be easily replayed [dev/stage/prod]
It can be version controlled   

   TERRAFORM WORK FLOW  
Terraform init
terraform validate
Terraform plan
Terraform apply
Terraform import, refresh, taint, destroy, fmt 

10 HIGH LEVEL BLOCKS
Terrafform settings blk
providers blk
Resource blk
Input variables blk
import block
data sources blk
output values blk
local values blk
modules blk
Import block



FOR HIGH RELIABILITY
Monitoring, automated backups

    FOR HIGH AVAILABILITY
I deploy applications to multiple avilability zones in AWS with an application load balancer distributing traffic & implement autoscaling groups for EC2 instancs & kubernetesPodAutoscaler to dynamically adjust capacity based on demand.
Multicloud deployment ie AWS. GCP 
Multiple servers

Also expose our application using kubenetes service objects like clusterip for internal communication & nodeport, externalname, lb, & ingress for external communication. However bc load balancers are expensive service in AWS, In my env we deploy ngnix ingress
controller in our cluster to route traffic to the backend application, ngnix ingress is a layer7 LB, so it can suppot multiple backend application. after that we create a service like NgnixSVC to serve as our frontend and after that based on the service, we create
some ingress rules which determines how traffic is routed.
But for us to create the rules, in AWS we have to create route53 , Arecordset, & we can either use hostbase routing or pathbased routing & then we deploy the ingress rule we have defined.





